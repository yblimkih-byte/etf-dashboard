# CHANGELOG

형식: `vN (배포일) — 변경 요약`. Apps Script 배포 관리의 "버전 N" 과 동일 번호. 운영 조치(보정 실행 등)는 "운영" 항목에 기록.

- v10 (2026-09-15) — [상위 5개사·시장 유형 비중] 시장 전체 | 상위 5개사 | 선택 운용사 그룹 사이 반 행 간격(`catPos`), 표 구분선; 개별 운용사 목록에서 상위 5개사 제외(`apiMeta_.mgrs`); bar chart race 채권/금리형 종목 회색(`apiRace_.bond`, `App.BOND_GRAY`)
- v9 (2026-09-13) — KRX 당일분 NAV 미게시(전 종목 0) 응답을 빈 응답으로 처리(`fetchEtfDaily_`); "NAV 0 일자 보정" 메뉴(NAV 기준, 일별요약·당월 스냅샷 포함 제거 후 재집계)
  - 운영: 2026-09-11 NAV 0 블록 제거, 2026-09 스냅샷 09-10 기준 재생성
- v8 (2026-09-11) — 2행 막대 증감 레이블을 계열선 사이 정중앙·계열 색으로, 운용사 막대 높이 확대·2줄 레이블
- v7 (2026-09-11) — 증감 레이블 생략 없음(띠 분리), M/S 영역차트 마지막 값 위치, 유형 레이블 `00.0조/(00.0%)`, 표 구분선, 목록 접힘 애니메이션, race 900ms
- v6 (2026-09-11) — 지수 선차트 X축 정렬, 표 지수 열 연초 대비, 증감 레이블 2줄, 운용사 칩 초기화·목록 필터
- v5 (2026-09-10) — 증감률 소수 1자리, 헤더 고정, 지수 비교 선차트, 2행 누적 막대, M/S 영역차트, 신규상장 NAV 막대
- v4 (2026-09-10) — 운용사 매칭(범례_운용사 브랜드), agg_월말요약·일별요약, API 캐시, 운용사/상장일 보정 메뉴

## v14 (Apps Script) · v107 (Vercel) (2026-09-22) — 유형별 NAV 탭: 월별 비중 차트 삭제, ETF 히트맵 신설
- 사용자 요구: finviz.com/map 식. 유형(국내주식형·해외주식형·채권형…) 넓이 = NAV, 그 안 박스 = 개별 ETF(중분류 없음). 이름은 브랜드+명칭 앞부분, 아래 숫자 = 전년말 대비 NAV 증감액(조원), 마우스 오버 시 공식명칭·금년(신규상장은 상장일 이후) 증가액
- `Api.gs` `treemap` 액션(`apiTreemap_`): 기준일·전년말 스냅샷 대조, 상장일 > 전년말이면 isNew(기준 0). `Aggregate.gs` warmParams_ 에 treemap 추가
- `Tabs.html`: 유형 탭 2절 교체. `squarify()`(면적 비례 배치), `treemap()`(DOM 박스, 색 = 증감 발산 스케일: 상위 10% 절대증감을 최대 채도, 면적 900px² 미만은 '기타 n종목'으로 묶음, 커스텀 툴팁, 리사이즈 재배치). 데이터는 탭 본문 로드 후 별도 호출
- `Style.html`·`web.css`: .treemap-host/.tm-* 스타일. 양쪽 화면 공통
- `api/data.js`: 프록시 허용 action 에 `treemap` 추가(누락 시 Vercel 에서 400)

## v13 (Apps Script) · v106 (Vercel) (2026-09-21) — 기준일 변경 시 첫 조회 지연 해소
- 증상: Vercel 에서 기준일을 방금 적재된 2026-09-18 로 바꾸면 탭마다 오래 걸림. 실측(캐시 없이): overview 1.5s · byMgr 2.5s · shares 2.5s · topEtf 5.2s · byType 5.6s · race 7.6s · newListings 11.3s · turnover 14.0s. 캐시 적중 시 0.04~0.1s
- 원인: ① 적재 후 예열(`warmCache_`)이 `api(a, {})` 로 호출 → 캐시 키가 파라미터 JSON 이라 화면 요청(`{date:…}`)과 달라 **예열이 한 번도 적중하지 않았음**(Apps Script 화면도 동일) ② 기본 기준일(전월말)이 아닌 날짜는 애초에 예열 대상이 아님 ③ CacheService 보존 6시간 → 오후에는 다시 첫 조회가 느려짐 ④ 기준일을 바꾸면 현재 탭만 받아 탭 전환 때마다 대기
- v13 `Aggregate.gs`: `warmAll()`(+`warmParams_`, `cont_warmAll`) — 화면이 보내는 파라미터 형태 그대로, 최근 영업일 → 전월말 → 당월 나머지 일자 순으로 예열. 적재 직후 별도 실행(1분 뒤)으로 돌고, 시한(4.5분) 내 못 끝내면 2분 뒤 이어서, 끝나면 5.5시간 뒤 스스로 재예약
- v106 `web/src/shell.js`: 조건이 바뀌면 나머지 탭을 같은 조건으로 백그라운드 선조회(값이 비어 있는 조건의 탭은 제외). `api/data.js`: meta 의 CDN 캐시 10분→1분(새 기준일이 곧바로 목록에 보이도록). 주석 오타 정정
- test/warm_test.js: 예열 호출 형태 점검

## v12 (2026-09-21 15:39, Apps Script) — 적재 안정화: 감시 트리거·미게시 낮 재시도·단계 로그·KRX 재시도
- 배경: 09-21 08:31 `loadDaily` 가 실행 제한(6분)으로 강제 종료 → 금요일(09-18)분이 19:00 까지 비어 있었음. 최근 7일간 같은 시간 초과 7건, 강제 종료 시 시트 로그가 남지 않아 멈춘 단계 불명
- `Util.gs`: `step_()`(Cloud 로그 단계 표식), 감시 트리거 `armWatchdog_/disarmWatchdog_/wd_loadDaily`(적재 시작 시 12분 뒤 1회성 트리거를 걸고 정상 종료 시 해제 → 강제 종료되면 자동 재시도, 연속 3회 한도), `scheduleUnpublishedRetry_`(전영업일분 미게시 시 08~16시에는 90분 뒤 재시도, 하루 3회)
- `Load.gs`: `loadDaily` 에 감시 설정·해제, 단계 표식 10여 곳, 미게시 재시도 예약
- `Krx.gs`: `krxGet_` 연결 실패·5xx 시 3초 뒤 1회 재시도
- 정기 트리거(08:30 / 19:00)는 그대로. 웹앱 화면·API 변경 없음. test/shim.js: `Utilities.sleep`, `formatDate(...,'H')`

## v105 (2026-09-21) — Vercel 화면 글꼴 Pretendard 단일화
- 사용자 지시: 모든 글꼴을 Pretendard 계열로 통일. `--mono` 를 `--font` 로 대체(표 헤더·레이블·순위 등 고정폭 표기 폐지), 숫자 정렬은 `tnum`. 차트(캔버스) 글자는 웹폰트 로드 후 재그림

## v104 (2026-09-21) — Vercel 화면: shadcn/ui dashboard 구성 + 차트 경량화
- v103 도 사용자 체감 개선 없음 → 참고를 ui.shadcn.com/examples/dashboard 로 변경. 원인 진단: 화면의 대부분인 차트·표 질감이 v101~v103 내내 동일했음
- 골격: 회색 사이드바(로고·아이콘 탭·자료·적재 상태, 토글로 접힘/모바일 서랍) + 흰 inset 본문 패널, 얇은 상단 헤더(토글·탭 제목·컨트롤)
- 지표 카드(`HEAD[탭].cards`): 설명·큰 수치·추세 배지(↗/↘)·하단 2줄, 옅은 그라데이션·shadow-xs. 개관 6(3열)·운용사 5·유형 6·비중 4·상위 ETF 6·신규 4·거래대금 4. 운용사 탭 M/S 위치 눈금 카드 유지
- 섹션 = 카드(제목 아래 설명), 표 = 둥근 테두리·연회색 헤더·일반 표기(모노 대문자 폐지), 칩·버튼·셀렉트 shadcn 형태
- 차트 경량화(`A.chart` 래핑, 이 화면 한정 · 사용자 선택 (가): 레이블·수치·색 체계 유지): 단일 계열 검정 막대 #17171c→#3f3f46·기준 막대 #d4d4d8, 막대 모서리 6px·폭 축소, 격자 #f1f1f3·축선 제거, 눈금 #a1a1aa, 흰 카드형 툴팁, 둥근 범례
- test/shoot.js: 900px 이하에서는 `App.show()` 로 탭 전환(탭이 서랍 안에 있음)

## v103 (2026-09-21) — Vercel 화면 재개편: 정렬된 패널 시스템 (참고: irislabs.vercel.app 의 구성 원리)
- v102(좌측 레일·명조 결론 문장)는 사용자 기각. 상단 내비 + 중앙 1152px 컨테이너로 모든 블록의 좌우 끝을 같은 선에 정렬
- 페이지 헤드: 영문 킥커 · 탭 제목 · 설명 문장(API 값으로 자동 생성) · 상태 줄(기준일·최종 적재·갱신 주기). 컨트롤은 헤드 아래 고정 행(테두리형 셀렉트)
- 지표 타일: 탭마다 균등 폭 타일 행(`HEAD[탭].tiles`, 운용사 색 점·관찰 대상 `FOCUS` 테두리 강조) + 운용사 탭 M/S 위치 눈금. 개관 탭 기본 KPI 는 타일로 대체
- 섹션 = 1px 테두리 패널(제목행 좌: 제목 / 우: 주석, 하단 출처 줄). 그림자·등장 효과 없음, 밝은 바탕 유지, 차트 종류·색 변경 없음
- 글꼴 Pretendard + JetBrains Mono(레이블). 760px 이하: 내비 가로 스크롤, 타일 2열, 패널 내 가로 스크롤

## v102 (2026-09-21) — Vercel 화면 디자인 개편: 편집형 지면 + 분석툴 밀도
- 방향: 데이터 저널리즘 지면(결론 문장 제목·괘선·출처 줄) 기반, 분석툴형 좌측 어두운 레일·고밀도 표, 지표 모듈형 리드(제목+설명+눈금) 가미. 카드·그림자·둥근 모서리·등장 효과 제거
- `web/src/shell.js`: 탭별 리드 자동 생성(LEDE: overview·mgr·type·shares·top·new·turnover — API 응답 값만 사용, 관찰 대상 `FOCUS='한투'`), 수치 띠(band), M/S 위치 눈금(posStrip: 기준일 ●·전년말 ○), 섹션 출처 줄(data-src), Alt+↑↓ 탭 이동
- `web/src/web.css`·`index.html`: 좌측 레일(번호 탭), 상단 컨트롤 바(밑줄형 셀렉트), 명조 제목(Noto Serif KR)+Pretendard 본문+JetBrains Mono 레이블, 섹션 번호·2px 괘선, 표 13px 밀도, 980px 이하 상단 가로 탭
- `test/build_web.js`: 로컬 글꼴(node_modules) 주입. 차트·표 로직(gas/App·Tabs)은 변경 없음

## v11 (2026-09-21) — Vercel 프런트엔드 병행
- `Api.gs` doGet: `?action=&p=` JSON 엔드포인트 분기(없으면 기존 화면). `App.html` call(): `window.API_BASE` fetch 분기
- 신규: `api/data.js`(프록시·CDN 캐시, GAS_URL 환경변수), `vercel.json`, `web/`(build.js · src/index.html · web.css · shell.js · favicon.svg), `test/build_web.js`, `docs/Vercel_배포_가이드.md`
- 외형: 스톤 배경+흰 카드 구획, 헤더 압축(블러·스크롤 그림자), KPI 카드(증감 표시), 스켈레톤 로딩·진입 전환, 필 셀렉트, 모바일(탭 가로 스크롤·차트 가로 스크롤·표 첫 열 고정), 방향키 탭 이동·뒤로가기·탭별 문서 제목·맨 위로
- `test/shoot.js`: 인자(페이지·출력폴더·폭), CHROME_PATH


